{
  "name": "camera-filters",
  "version": "1.0.0",
  "description": "Streaming from a Canvas with Filters to an OpenTok Session",
  "main": "index.js",
  "scripts": {
    "postinstall": "webpack --entry ./src/index.js --output-filename --devtool source-map index.js",
    "watch": "webpack --entry ./src/index.js --output-filename index.js --devtool source-map --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "canvas",
    "opentok",
    "filters"
  ],
  "author": "Adam Ullman",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^2.13.0",
    "eslint-config-airbnb-base": "^3.0.1",
    "eslint-plugin-import": "^1.8.1"
  },
  "dependencies": {
    "tracking": "^1.1.2"
  }
}
